// Stub for Next.js's `server-only` package. Importing it in a server module
// throws at build-time if the module gets bundled into a client chunk; under
// vitest there's no such concern, so this file is intentionally empty.
export {};
